       <Window x:Class="WinFolderList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="300"
        Width="300"
        Title="Win Folder List"
        DataContext="{Binding Main, Source={StaticResource Locator}}">
    

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock/>
            <Button Grid.Column="1" Content="Browse" HorizontalAlignment="Right" VerticalAlignment="Top" Command="{Binding Browse}" IsEnabled="False"></Button>
            <Button Grid.Column="2" Content="Scan" HorizontalAlignment="Right" VerticalAlignment="Top" Command="{Binding Scan}"></Button>
        </Grid>
        
        
     

    </Grid>
</Window>
